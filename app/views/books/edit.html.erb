<%= render 'shared/header_admin' if user_signed_in?  %>


    <%= link_to "Back to books", books_path, class:'btn btn-light m-3 bi bi-arrow-90deg-left text-dark'  %>

<div class="container rounded">

    <h3 class="text-center">Edit book</h3>

    <%= render 'shared/books/form' if user_signed_in? && current_user.role_id == 1  %>
</div>