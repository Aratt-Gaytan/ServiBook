
</strong>
<%= render 'shared/header_admin' if user_signed_in?  %>

<%= link_to "New Book", new_book_path, class: 'btn btn-light m-3 bi bi-arrow-90deg-left text-dark' if current_user.role_id == 1%>

<div class="container p-4 mt-4">
  <h1 class="text-center">Books</h1>
  <div class="row">

  <% @books.each do |book| %>
    <div class="col mb-3">
      <div class="card " style="width: 155px;">
          
          <% if book.photo.attached? %>
            <%= image_tag(book.photo,  class: 'card-img-top', height: "155px")  %>
          <% else %>
            <%= image_tag 'defaultBook.jpg', class: 'card-img-top' %>
          <% end %>
          
        <div class="card-body">
         <strong> <%= link_to "#{book.title} (#{book.author.first_name} #{book.author.last_name})", book_path(book) ,class: 'card-text' %></strong>

        </div>
      </div>
    </div>
    
     
  <% end %>

  </div>
</div>