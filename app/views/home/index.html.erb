<%= stylesheet_link_tag 'home' %>

<% if user_signed_in? %> 
    <p>Welcome, 
        <%= current_user.email %>
        <%= current_user.name %>
        <%= current_user.phone %>
        <%= current_user.address %>
        rol: <%= current_user.role %>

    </p> 
    
    <%= link_to "Sign out", destroy_user_session_path, method: :delete %>

 <% else %> 
    <p> No haz iniciado sesion tonto te amo</p>
    <%= link_to "Sign in", new_user_session_path %>
<% end %>