<%= stylesheet_link_tag 'home' %>

<%= render 'shared/header_admin' if user_signed_in? && current_user.role_id == 1  %>
<%= render 'shared/principal_page' if user_signed_in? && current_user.role_id == 1  %>




