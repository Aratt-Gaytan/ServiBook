<%= render 'shared/header_admin' if user_signed_in? && current_user.role_id == 1  %>
    <%= link_to "Back to authors", authors_path, class:'btn btn-outline-primary m-3 bi bi-arrow-90deg-left'  %>
    

<h3 class="text-center">Create an Author</h3>

<%= render 'shared/authors/form' if user_signed_in? && current_user.role_id == 1  %>

