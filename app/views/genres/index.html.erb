<%= render 'shared/header_admin' if user_signed_in?   %>

<%= link_to "New genre", new_genre_path, class: 'btn btn-light m-3 bi  bi-plus-lg text-dark' if current_user.role_id == 1 || 3%>

<div class="container p-4 mt-4">
  <h1 class="text-center">Genres</h1>
  <div class="row">

  <% @genres.each do |genre| %>
    <div class="col-2 mt-2">
      <div class="card" style="width: 9rem;">
          
          <% if genre.photo.attached? %>
            <%= image_tag(genre.photo,  class: 'card-img-top', height: "130px")  %>
          <% else %>
            <%= image_tag 'default.jpg', class: 'card-img-top' %>
          <% end %>
          
        <div class="card-body">
          <%= link_to "#{genre.name} ", genre_path(genre) ,class: 'card-text' %>
        </div>
      </div>
    </div>
    
     
  <% end %>

  </div>
</div>