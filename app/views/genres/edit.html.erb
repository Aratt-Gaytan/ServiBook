<%= render 'shared/header_admin' if user_signed_in? %>



    <%= link_to "Back to authors", genres_path, class:'btn btn-light m-3 bi bi-arrow-90deg-left text-dark'  %>

<div class="container rounded">

    <h3 class="text-center">Edit <%= "#{@genre.name} "%></h3>

    <%= render 'shared/genres/form' if user_signed_in? && current_user.role_id == 1  %>
</div>
